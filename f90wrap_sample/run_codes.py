import distance
import velocity

print('Distance: ', distance.distance_module.distance( 20.0, 2.0 ))
print('Velocity: ', velocity.velocity_module.velocity( distance.distance_module.distance( 20.0, 2.0 ), 2.0) )

