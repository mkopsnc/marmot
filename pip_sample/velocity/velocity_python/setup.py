from setuptools import setup, Extension

setup(name = 'velocity_wrapper',
  author = 'Michal K. Owsiak', 
  version = '0.1.0',
  description="An example of Python wrapper for velocity code",
  include_package_data=True,
  packages=['velocity_wrapper'])
