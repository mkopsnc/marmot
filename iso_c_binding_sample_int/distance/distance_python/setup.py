from setuptools import setup, Extension

setup(name = 'distance_wrapper',
  author = 'Michal K. Owsiak', 
  version = '0.1.0',
  description="An example of Python wrapper for distance code",
  include_package_data=True,
  packages=['distance_wrapper'])
